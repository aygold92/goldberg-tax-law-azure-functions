# The name drives the mount path the skill reads: /mnt/memory/bank-patterns/
# Written by bank-statement-splitting; consolidated by memory-consolidation.
name: bank-patterns
description: >
  Bank statement identification patterns and boundary detection rules. One file per bank type
  (e.g. /bank_of_america.md). Each file describes how to identify that bank and how to find
  statement boundaries within it.
